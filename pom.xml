<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <artifactId>cloud-ready-parent</artifactId>
    <groupId>top.infra</groupId>
    <modelVersion>4.0.0</modelVersion>
    <packaging>pom</packaging>
    <parent>
        <artifactId>build-docker</artifactId>
        <groupId>cn.home1</groupId>
        <version>0.2.2</version>
    </parent>
    <version>1.0.2.U1</version>

    <!-- @formatter:off -->
    <dependencies>
        <dependency><groupId>javax.servlet</groupId><artifactId>javax.servlet-api</artifactId><scope>provided</scope></dependency>

        <dependency><groupId>org.apache.commons</groupId><artifactId>commons-lang3</artifactId></dependency>
        <dependency><groupId>org.projectlombok</groupId><artifactId>lombok</artifactId><scope>provided</scope></dependency>

        <dependency><groupId>cn.home1</groupId><artifactId>logback-test-config</artifactId><scope>test</scope></dependency>
        <dependency><groupId>org.springframework.boot</groupId><artifactId>spring-boot-starter-test</artifactId><scope>test</scope></dependency>
    </dependencies>
    <!-- @formatter:on -->

    <dependencyManagement>
        <!-- @formatter:off -->
        <dependencies>
            <dependency>
                <artifactId>deps-spring-boot-${spring-boot.version}</artifactId>
                <groupId>cn.home1</groupId>
                <scope>import</scope>
                <type>pom</type>
                <version>${deps-spring-boot.version}</version>
            </dependency>
            <dependency>
                <artifactId>testcontainers-bom</artifactId>
                <groupId>org.testcontainers</groupId>
                <scope>import</scope>
                <type>pom</type>
                <version>${testcontainers.version}</version>
            </dependency>

            <!--<dependency><groupId>cn.home1</groupId><artifactId>logback-logstash-config</artifactId><version>${log-config.version}</version></dependency>-->
            <dependency><groupId>cn.home1</groupId><artifactId>logback-test-config</artifactId><version>${log-config.version}</version><scope>test</scope></dependency>

            <dependency><groupId>top.infra</groupId><artifactId>spring-boot-commons</artifactId><version>${spring-boot-commons.version}</version></dependency>
            <dependency><groupId>top.infra</groupId><artifactId>spring-boot-test-support</artifactId><version>${spring-boot-test-support.version}</version></dependency>
        </dependencies>
        <!-- @formatter:on -->
    </dependencyManagement>

    <modules>
        <module>spring-boot-commons</module>
        <module>spring-boot-test-support</module>
    </modules>

    <properties>
        <deps-spring-boot.version>0.1.6</deps-spring-boot.version>
        <log-config.version>0.1.2</log-config.version>
        <spring-boot.version>1.5.12.RELEASE</spring-boot.version>

        <spring-boot-commons.version>1.0.2.U1</spring-boot-commons.version>
        <spring-boot-test-support.version>1.0.2.U1</spring-boot-test-support.version>

        <testcontainers.version>1.8.2</testcontainers.version>
    </properties>

    <!-- @formatter:off -->
    <repositories>
        <!-- Make artifacts in OSSRH repositories available -->
        <repository>
            <id>OSSRH-releases</id><name>central releases</name><url>https://oss.sonatype.org/content/repositories/releases/</url>
            <releases><enabled>true</enabled></releases><snapshots><enabled>false</enabled></snapshots>
        </repository>
        <repository>
            <id>OSSRH-snapshots</id><name>central snapshots</name><url>https://oss.sonatype.org/content/repositories/snapshots/</url>
            <releases><enabled>false</enabled></releases><snapshots><enabled>true</enabled></snapshots>
        </repository>
    </repositories>
    <!-- @formatter:on -->
</project>
